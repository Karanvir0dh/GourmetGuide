<% layout('layouts/boilerplate.ejs') %>

<div class="row">
    <h1 class="text-center">Edit Restaurant</h1>
    <div class="col-6 offset-3">
        <form action="/restaurants/<%= restaurant._id %>?_method=PUT" method="POST">
            <div class="mb-3">
                <label class="form-label" for="name">Restaurant Name</label>
                <input class="form-control" type="text" name="restaurant[title]" id="name"
                    value="<%= restaurant.title%>">
            </div>

            <div class=" mb-3 ">
                <div class=" input-group">
                    <label class="input-group-text" for="priceRange">Price Range</label>
                    <select class="form-select" id="priceRange" name="restaurant[priceRange]">
                        <option value="$" <%= restaurant.priceRange === '$' ? 'selected': '' %>>$</option>
                        <option value="$$" <%= restaurant.priceRange === '$$' ? 'selected': '' %>>$$</option>
                        <option value="$$$" <%= restaurant.priceRange === '$$$' ? 'selected': '' %>>$$$</option>
                    </select>
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label" for="location">Location</label>
                <input class="form-control" type="text" name="restaurant[location]" id="location"
                    value="<%= restaurant.location%>">
            </div>

            <div class=" mb-3">
                <label class="form-label" for="image">Image URL</label>
                <input class="form-control" type="text" name="restaurant[image]" id="image"
                    value="<%= restaurant.image%>">
            </div>

            <div class=" mb-3">
                <label class="form-label" for="description">Description</label>
                <textarea class="form-control" type="text" name="restaurant[description]"
                    id="description"><%= restaurant.description%></textarea>
            </div>

            <button class=" mb-3 btn btn-success">Save</button>

        </form>
        <a href="/restaurants/<%= restaurant._id %>" class="btn btn-dark">Go back</a>
    </div>
</div>